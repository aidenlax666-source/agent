docs: HTTPS 完整部署配置——Nginx TLS + certbot 自动续期 + 静态缓存

云端部署此前只提到"Nginx/LB"但无实际配置。新增 deploy/ 三件套：

- deploy/nginx-https.conf：HTTP→HTTPS 跳转、TLS 1.2/1.3 加固、HSTS/
  nosniff/X-Frame-Options 安全头、前端(:3000)/API(:8000)/产物(:8001)
  三路转发、产物静态长缓存、视频音频 Range 流式放行、WebSocket 升级
- deploy/init-certbot.sh：Let's Encrypt 首次签发 + 启用 certbot.timer
  自动续期（每天检查，到期前 30 天续）
- deploy/README.md：部署步骤 + 后端 TRUSTED_PROXY_IPS/CORS 同步说明

DEPLOYMENT.md 补 HTTPS 章节。纯配置/文档，无代码改动。