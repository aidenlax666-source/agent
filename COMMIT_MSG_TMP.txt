ci: GitHub Actions 流水线——PR 跑测试 + main 构建镜像推送 GHCR

此前无任何 CI/CD：测试只在本地跑，镜像手工构建，部署全靠手动。
新增 .github/workflows/ci.yml：

- backend-test（PR/push main）：Python 3.11 + pytest 全量
  （FakeRedis 模拟云模式，无需真实 Redis/Postgres）
- frontend-build（PR/push main）：Next.js 生产构建
- build-images（main 且前两者通过）：构建 backend/frontend 镜像
  推送 GHCR（ghcr.io/<repo>/backend:latest + sha 标签）

DEPLOYMENT.md 补 CI/CD 章节（流水线表 + 服务器拉镜像/自动更新说明）。