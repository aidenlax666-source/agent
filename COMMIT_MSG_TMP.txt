feat: 任务过程日志 steps 落库——跨实例一致、worker 崩溃不丢

此前 steps（过程可见性：每个执行步骤）只存在执行实例的内存 _TASKS 里，
未写 SQLite：任务在实例 B 跑、用户轮询打到实例 A 拿不到过程日志；
worker 崩溃/重启后过程日志全丢。且发现白名单里有 progress 但表结构
无该列（传 progress 会报 no such column）的隐藏 bug。

改动：
- database.py：mini_tasks 新增 steps 列（JSON 数组）与 progress 列（修复
  隐藏 bug），含老库迁移；_save_mini_task 存 steps；白名单加 steps；
  _update_mini_task 对 steps/progress 正确处理；_get_mini_task /
  _list_mini_tasks / reaper 读取均解析 steps
- mini_tasks.py：_run_task 每个执行模式开始时把 steps+progress 落库
  （update_mini_task），跨实例可见、崩溃不丢；get_status 内存/DB 两条
  路径都返回 steps（DB 路径解析 JSON 字符串）

测试 +4（落库与增量更新/模拟重启跨实例读取/空默认/get_status 返回），
共 118 全过。