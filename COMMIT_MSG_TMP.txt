feat: 全局 API 限流——Redis 计数器跨实例共享（无 Redis 回退内存）

匿名提交限速此前是进程内内存表（_ANON_SUBMIT_RATE），多实例下每实例
各自计数——用户轮流打到不同实例即可绕过限额，与沙箱并发是同类问题。

新增 distributed.rate_limit(key, limit, window)：Redis INCR+EXPIRE 固定
窗口计数，key 跨实例共享；Redis 不可用时回退进程内计数（单机行为与原来
一致）；Redis 抖动时放行（避免误伤用户）。

接入：_check_anon_rate 改调 rate_limit("anon:{ip}")，删除内存 deque 表
与旧常量（_ANON_RATE_MAX_KEYS 等）。

测试 +4（Redis 计数/隔离/窗口重置/单机回退），共 87 全过。
