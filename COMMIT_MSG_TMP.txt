feat: 对象存储适配——产物存储抽象层（本地/共享卷/S3 可切换）

多实例下产物存取此前直接读写 web/ 本地/共享卷文件：流式/下载/打包全部
依赖挂载，不想挂共享卷或要无限容量时无解。新增存储抽象层：

- 新增 app/services/storage.py：统一接口 artifact_exists/size/read/
  download_response/stream_response/file_response
  - local（默认）：web/ 目录或共享卷挂载点，现状行为不变（含防路径穿越）
  - s3（STORAGE_BACKEND=s3 + S3_* 配置）：boto3，跨实例天然一致、不限
    磁盘、CDN 友好；流式走 S3 Range 请求（边下边播），下载走代理
- config：storage_backend / s3_bucket / s3_endpoint / s3_access_key /
  s3_secret_key / s3_region（MinIO 兼容：填 endpoint 即可）
- mini API：download/stream 改走存储抽象；output_file 支持 web/ 相对
  路径（S3 key）与本地绝对路径（本地模式兼容旧行为）

测试 +9（本地 exists/size/read、下载头、Range 206/416、S3 exists/size/
read、S3 Range 透传），共 127 全过。