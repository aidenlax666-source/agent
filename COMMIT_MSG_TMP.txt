feat: 系统可观测性——任务统计 + worker 健康 + 队列深度 + 监控页

运维视角看系统是否健康：worker 是否在跑、队列是否积压、任务成功率。

后端：
- distributed：worker_heartbeat()（worker 循环每轮刷新，TTL 120s 失联自清）+
  list_workers() + queue_depth()（普通/高优分开统计）
- database：task_stats() 聚合总量/今日/状态分布/成功率/平均耗时
- mini API：GET /api/system/stats 汇总三者（单机模式 workers/queue 为空但接口可用）

前端：
- 新页面 /monitor：核心指标卡（总量/成功率/平均耗时/运行模式）+ 状态分布
  + worker 列表（含失联告警"没有活跃 worker"）+ 队列深度；15s 自动刷新
- AppNav 加入口

测试 +4（统计聚合/worker 心跳与失联/队列深度/单机模式），共 101 全过。
