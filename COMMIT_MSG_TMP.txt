chore: 整理仓库结构——工具脚本收进 cli/ 目录，根目录只留必要文件

根目录此前散落 agent-cli.py（CLI 开发助手）、local_worker.py（本地执行端
源码）、build_local_worker.ps1（打包脚本），与 frontend/backend/配置混在
一起。收进 cli/ 目录：

- git mv agent-cli.py cli/agent-cli.py（保留历史）
- git mv local_worker.py cli/local_worker.py
- git mv build_local_worker.ps1 cli/build_local_worker.ps1

根目录现在只保留必要文件：README×2、.env.example、.gitignore、
docker-compose×2、start.ps1、LICENSE、CONTRIBUTING、CODE_OF_CONDUCT、
setup-git-hooks×2。

同步更新所有引用（README/en、docs/DEPLOYMENT、setup-git-hooks 的
pre-commit 路径检测）；.gitignore 补 cli/dist|build|*.spec|配置；
删除误追踪的 COMMIT_MSG_TMP.txt。测试 140 全过。